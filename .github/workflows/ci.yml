name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Termux Environment
        run: echo "Simulating Termux environment..."
      - name: Run placeholder tests
        run: bash tests/test_structure.sh

  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Update README.md dynamically
        run: |
          echo "Updating README.md..."
          # zde bude volání final_readme.sh
