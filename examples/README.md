# Examples

This directory will contain runnable examples and test cases
for Termux-Updater modules.

Future examples:
- `update_dryrun.sh` — simulate an update
- `verify_token.sh` — check token status
