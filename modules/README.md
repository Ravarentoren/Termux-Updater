# Modules

Each module extends Termux-Updater functionality.

Suggested modules:
- `github-sync`: Sync repositories and configs
- `system-check`: Diagnostics for Termux / Linux environments
- `notifier`: Token expiration and issue creation handler
